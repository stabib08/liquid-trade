# Pinned for reproducibility. Target: CPython 3.12 (see .python-version).
# Rebuild a clean env with:  python3.12 -m venv .venv && ./.venv/bin/pip install -r requirements.txt
duckdb==1.1.3
yfinance==1.5.1        # native curl_cffi impersonation; older 0.2.x hits Yahoo 429
curl_cffi==0.15.0      # browser impersonation used by yfinance to clear bot-flavored 429s
pandas==2.2.3
numpy==2.1.3
scipy==1.14.1
statsmodels==0.14.4
pyarrow==18.1.0
pyyaml==6.0.2
